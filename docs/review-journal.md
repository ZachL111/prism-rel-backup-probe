# Review Journal

The cases below are the review handles I would use before changing the implementation.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its reliability focus without claiming live deployment or external usage.

## Cases

- `baseline`: `budget pressure`, score 181, lane `ship`
- `stress`: `failure width`, score 246, lane `ship`
- `edge`: `recovery gap`, score 174, lane `ship`
- `recovery`: `runbook drift`, score 195, lane `ship`
- `stale`: `budget pressure`, score 152, lane `ship`

## Note

The repository should be understandable without pretending it is larger than it is.
